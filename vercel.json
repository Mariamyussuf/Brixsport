{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/api/proxy(.*)",
      "dest": "/api/proxy"
    },
    {
      "src": "/api/(.*)",
      "dest": "/api/proxy?path=$1"
    }
  ]
}